# Django RESTAPI ToDo application[WIP]

A ToDo application to practice building an API with Django REST Framework.

Project will need to regenerate a new SECRET_KEY because it 
was exposed previously.